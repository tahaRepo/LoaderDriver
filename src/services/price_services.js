import { myAxios } from "./helper";

export const updatePrice=async (foodCost, fuelCost, vehicleMaintenanceCost)=>{
    // const response = await myAxios.put(`expense/${foodCost}/${fuelCost}/${vehicleMaintenanceCost}`);
    console.log('functioncalled');
    return true;
}